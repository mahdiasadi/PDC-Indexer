<Window x:Class="ProjectIndexer.Wpf.ArchivePickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Load Archive" Height="450" Width="650"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8">
            Select an archive to load:</TextBlock>

        <ListView Grid.Row="1" x:Name="ArchiveList" SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Drive" Width="60" DisplayMemberBinding="{Binding DriveLetter}"/>
                    <GridViewColumn Header="Date" Width="140" DisplayMemberBinding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}"/>
                    <GridViewColumn Header="Entries" Width="100" DisplayMemberBinding="{Binding EntryCount, StringFormat={}{0:N0}}"/>
                    <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding FileSize, StringFormat={}{0:N0} B}"/>
                    <GridViewColumn Header="Label" Width="200" DisplayMemberBinding="{Binding DriveLabel}"/>
                </GridView>
            </ListView.View>
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock FontWeight="Bold" FontSize="14"
                                       Text="{Binding Name}" Margin="0,4,0,2"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Load Selected" Width="110" Height="30" Margin="0,0,10,0"
                    IsDefault="True" Click="Load_Click"/>
            <Button Content="Cancel" Width="80" Height="30" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
